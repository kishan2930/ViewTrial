import {Text, View} from 'react-native';
import React from 'react';
import {styles} from './shortlistStyles';

const Shortlist = () => {
  return (
    <View style={styles.container}>
      <Text>Shortlist Screen</Text>
    </View>
  );
};

export default Shortlist;
